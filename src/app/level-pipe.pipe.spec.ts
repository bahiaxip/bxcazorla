import { LevelPipePipe } from './level-pipe.pipe';

/*
describe('IconTypePipe', () => {
  it('create an instance', () => {
    const pipe2 = new IconTypePipe();
    expect(pipe2).toBeTruthy();
  });
});
*/

describe('LevelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LevelPipePipe();
    expect(pipe).toBeTruthy();
  });
});


